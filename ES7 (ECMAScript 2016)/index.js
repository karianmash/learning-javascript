/**
 * Array.prototype.includes(): A new method added to the Array prototype 
 * that allows you to check whether an array includes a certain element.
*/

const numbers = [1, 2, 3, 4, 5];
const includesThree = numbers.includes();

console.log(includesThree); // Output: true